// we use random() to generate a random decimal number
// This is a number that is between 0 and 1 but not 1 as shown in the example below
function randomFraction() {

    return Math.random();
}

console.log(randomFraction());